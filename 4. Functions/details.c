#include <stdio.h>

void input_details(int *roll, int *age, int *marks)
{
    printf
}
int main()
{
    // Enter and print details of three students

    int n = 3;

    for (int i = 0; i < n; i++)
    {
        int roll, age, marks;
        input_details( &roll, &age, &marks );
        output_details();
    }

    return 0;
}
